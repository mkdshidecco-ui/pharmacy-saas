const http = require('http');

async function test() {
  const start = Date.now();
  // Assume tenantId is the first one found or we can just query the sqlite directly
  // Actually, I don't know the tenantId.
}
test();
